# Json grammar check

Version required: <Badge text="2.0.6" />

The upper right corner of the JSON parameter input box provides a JSON syntax check. If the JSON is incorrect, the corresponding error will be prompted. Of course, it also supports formatting and other operations.

![json](/img/json.png)
